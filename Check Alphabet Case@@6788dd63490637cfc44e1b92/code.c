#include <stdio.h>
int main() {
    char ch;
    scanf("%c",&ch);
    if(isupper(c)){
        printf("Uppercase");
    }else if(islower(c)){
        printf("Lowercase");
    else{
        printf("Not an alphabet");
    }
}
    return 0;
}