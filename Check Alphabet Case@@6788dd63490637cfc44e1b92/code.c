#include <stdio.h>
int main() {
    char ch;
    scanf("%c",&ch);
    if(isupper(c)){
        printf("Uppercase");
    }else if(is lower(c)){
        printf("Lowercase");
    else{
        printf("Not an aplphabet");
    }
}
    return 0;
}